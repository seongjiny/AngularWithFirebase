{
  "rules": {
    "quotes": {
      ".read": true,
      ".write": true,
      "$moviequote": {
        "movie": {
          ".validate": "newData.isString()"
        },
        "quote": {
          ".validate": "newData.isString()"
        },
        "$other": {
          ".validate": false
        }
      }
    }
  }
}
